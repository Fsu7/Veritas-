{
  "context": {
    "project": "XH-202630 科研文献智能助手 — 领域知识个性化生成与多智能体协同决策系统研究",
    "version": "v0.5",
    "milestone": "M5 / AM5：混合检索与功能完善",
    "requirement": "优化 LLM generate_stream() 首字节延迟，验证 < 2 秒（AM5 验收硬指标）。记录首字节时间戳并日志输出 first_token_latency_ms。流式输出失败时降级为非流式 generate()，不阻塞流程。支持 max_tokens/temperature 参数透传。提供基准测试脚本测量 10 次首字节延迟，输出 P50/P95/P99。本任务对应 AM5 Week 9 Day 6-7。",
    "requirement_ids": ["F3.3.2", "F3.3.5"],
    "reference_docs": [
      {"path": "docs/ai-service/AI服务模块系统架构文档.md", "purpose": "了解 LLM 三级降级架构和 generate_stream 接口"},
      {"path": "docs/ai-service/AI服务模块项目里程碑文档.md", "purpose": "确认 AM5 Week 9 Day 6-7 流式输出优化交付物和首字节<2秒硬指标"},
      {"path": "AGENTS.md", "purpose": "确认 LLM 降级约束和流式输出规范"}
    ]
  },
  "current_architecture": {
    "involved_layers": ["python_ai_service"],
    "related_modules": [
      {"layer": "python_ai_service", "module_path": "app.services.llm_service", "description": "LLMService 已实现三级降级（Builtin/API/Local）+ generate_stream()，但无首字节计时"},
      {"layer": "python_ai_service", "module_path": "app.core.config", "description": "Settings 配置类，需新增 LLM_STREAM_TIMEOUT"},
      {"layer": "python_ai_service", "module_path": "app.services.llm_service.BuiltinLLMProvider", "description": "内置 LLM Provider，generate_stream() 已实现"},
      {"layer": "python_ai_service", "module_path": "app.services.llm_service.APILLMProvider", "description": "API LLM Provider（DashScope/DeepSeek），generate_stream() 已实现"},
      {"layer": "python_ai_service", "module_path": "app.services.llm_service.LocalLLMProvider", "description": "本地 LLM Provider，generate_stream() 已实现"}
    ],
    "existing_implementations": [
      {"file_path": "Veritas/ai-service/app/services/llm_service.py", "description": "LLMService.generate_stream() 委托给 active_provider.generate_stream()，已有降级逻辑", "reuse_type": "extend"},
      {"file_path": "Veritas/ai-service/app/core/config.py", "description": "Settings 配置类已存在，需新增 LLM_STREAM_TIMEOUT 配置项", "reuse_type": "extend"}
    ]
  },
  "relevant_modules": [
    {
      "name": "LLMService",
      "path": "Veritas/ai-service/app/services/llm_service.py",
      "responsibility": "LLM 统一接口，三级降级，流式输出",
      "key_interfaces": [
        {"name": "generate_stream", "signature": "async def generate_stream(self, prompt: str, **kwargs) -> AsyncGenerator[str, None]", "description": "流式生成，yield token，需增加首字节计时"},
        {"name": "generate", "signature": "async def generate(self, prompt: str, **kwargs) -> str", "description": "非流式生成，流式失败时降级调用"},
        {"name": "_fallback_provider", "signature": "def _fallback_provider(self, failed_provider: str) -> Optional[BaseLLMProvider]", "description": "Provider 降级逻辑"}
      ]
    },
    {
      "name": "BaseLLMProvider",
      "path": "Veritas/ai-service/app/services/llm_service.py",
      "responsibility": "LLM Provider 抽象基类",
      "key_interfaces": [
        {"name": "generate_stream", "signature": "async def generate_stream(self, prompt: str, **kwargs) -> AsyncGenerator[str, None]", "description": "子类实现流式生成"}
      ]
    }
  ],
  "files_to_modify": [
    {"operation": "modify", "path": "Veritas/ai-service/app/services/llm_service.py", "description": "1) LLMService.generate_stream() 增加首字节计时：记录 stream_start_time，首个 token yield 前记录 first_token_time，计算 first_token_latency_ms 并 logger.info 输出；2) 流式失败（Provider 抛异常或超时）时降级调用 self.generate() 并 yield 完整结果；3) generate_stream 支持 max_tokens/temperature 参数透传到 provider.generate_stream()。", "layer": "python_ai_service"},
    {"operation": "modify", "path": "Veritas/ai-service/app/core/config.py", "description": "Settings 类新增 LLM_STREAM_TIMEOUT: int = 30（秒），支持环境变量覆盖。用于 generate_stream 的整体超时控制。", "layer": "python_ai_service"},
    {"operation": "create", "path": "Veritas/ai-service/tests/benchmark/llm_stream_latency_benchmark.py", "description": "基准测试脚本：1) 调用 LLMService.generate_stream() 10 次，每次记录首字节延迟；2) 输出 P50/P95/P99 统计；3) 输出 Markdown 报告；4) 验证 P95 < 2000ms（AM5 验收硬指标）。", "layer": "python_ai_service"},
    {"operation": "create", "path": "Veritas/ai-service/tests/test_llm_stream_optimization.py", "description": "单元测试：1) test_first_token_latency_logged 验证日志含 first_token_latency_ms；2) test_stream_fallback_to_non_stream 验证流式失败降级为非流式；3) test_max_tokens_temperature_passthrough 验证参数透传；4) test_stream_timeout_config 验证 LLM_STREAM_TIMEOUT 配置生效。", "layer": "python_ai_service"}
  ],
  "implementation_requirements": {
    "functional_requirements": [
      {"id": "FR-001", "description": "LLMService.generate_stream() 增加首字节计时：1) 在调用 provider.generate_stream() 前记录 stream_start_time = time.perf_counter()；2) 在首个 token yield 前记录 first_token_time，计算 first_token_latency_ms = (first_token_time - stream_start_time) * 1000；3) 使用 logger.info 输出 'first_token_latency_ms={value}'；4) 仅首个 token 计时，后续 token 不重复计时。", "priority": "P0", "acceptance_condition": "generate_stream() 日志包含 first_token_latency_ms"},
      {"id": "FR-002", "description": "首字节延迟 < 2000ms（AM5 验收硬指标）：1) 基准测试脚本测量 10 次首字节延迟；2) 计算 P95；3) 验证 P95 < 2000ms。若不达标，在基准测试报告中输出 WARNING 并建议优化方向（如预热连接、减少 prompt 长度）。", "priority": "P0", "acceptance_condition": "首字节延迟 P95 < 2000ms"},
      {"id": "FR-003", "description": "流式输出失败时降级为非流式 generate()：1) try-except 包裹 provider.generate_stream()；2) 捕获 Exception（含 TimeoutError、ConnectionError）；3) logger.warning 输出降级原因；4) 调用 self.generate(prompt, **kwargs) 获取完整结果；5) yield 完整结果（作为单个 token）；6) 不抛出异常，不阻塞流程。", "priority": "P0", "acceptance_condition": "流式失败降级为非流式，不抛出异常"},
      {"id": "FR-004", "description": "支持 max_tokens / temperature 参数透传：1) generate_stream(self, prompt, **kwargs) 接受 max_tokens 和 temperature；2) 透传到 provider.generate_stream(prompt, max_tokens=..., temperature=...)；3) 若 provider 不支持参数，忽略不报错。", "priority": "P1", "acceptance_condition": "max_tokens/temperature 参数正确透传到 provider"},
      {"id": "FR-005", "description": "基准测试脚本 tests/benchmark/llm_stream_latency_benchmark.py：1) 循环 10 次调用 LLMService.generate_stream()；2) 捕获日志中的 first_token_latency_ms（或自行计时）；3) 计算 P50/P95/P99；4) 输出 Markdown 报告到 tests/benchmark/reports/llm_stream_latency_report.md；5) 验证 P95 < 2000ms，不达标输出 WARNING。", "priority": "P1", "acceptance_condition": "基准测试脚本输出 P50/P95/P99 统计"}
    ],
    "degradation_requirements": {
      "llm_degradation": {"stream_failure": "流式失败降级为非流式 generate()，yield 完整结果", "provider_failure": "Provider 降级链：Builtin → API → Local（已有逻辑保持不变）"}
    }
  },
  "constraints": {
    "naming": {"python": {"class_name": "PascalCase", "function_variable": "snake_case", "constant": "UPPER_SNAKE_CASE", "file_name": "snake_case.py"}},
    "layering": {"python": {"rule": "LLM 服务在 services/，配置在 core/，基准测试在 tests/benchmark/，单元测试在 tests/"}},
    "error_handling": {"python": {"approach": "流式失败降级为非流式，不抛出异常", "llm_error_handling": "logger.warning + 降级到 generate()"}},
    "logging": {"python": "Loguru", "required_logs": ["first_token_latency_ms={value}"], "prohibited": ["在每个 token yield 时打印日志"]}
  },
  "forbidden_actions": [
    {"id": "FA-001", "action": "输出伪代码或 TODO 注释", "reason": "必须输出完整可执行代码", "severity": "critical"},
    {"id": "FA-002", "action": "修改三级降级链顺序（Builtin→API→Local）", "reason": "降级链已定义，仅增加流式→非流式降级", "severity": "high"},
    {"id": "FA-003", "action": "删除现有 generate() 非流式方法", "reason": "流式降级依赖 generate()", "severity": "critical"},
    {"id": "FA-004", "action": "在基准测试中调用真实 LLM API 且不 mock", "reason": "基准测试可调用真实 API，但需在 README 说明成本", "severity": "medium"},
    {"id": "FA-005", "action": "修改 Provider 子类的 generate_stream 签名", "reason": "仅修改 LLMService 层，Provider 层保持不变", "severity": "high"},
    {"id": "FA-006", "action": "引入 async_timeout 或 asyncio.wait_for 包裹流式生成", "reason": "流式生成是无限迭代器，不能用 wait_for 超时", "severity": "medium"}
  ],
  "test_requirements": {
    "unit_tests": [
      {"test_name": "test_first_token_latency_logged", "description": "验证 generate_stream() 日志含 first_token_latency_ms", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_stream_fallback_to_non_stream", "description": "mock provider.generate_stream 抛异常，验证降级到 generate()", "framework": "pytest", "coverage": ["error_flow", "degradation"]},
      {"test_name": "test_max_tokens_temperature_passthrough", "description": "验证 max_tokens/temperature 透传到 provider", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_stream_timeout_config", "description": "验证 LLM_STREAM_TIMEOUT 配置项可从环境变量读取", "framework": "pytest", "coverage": ["normal_flow"]}
    ],
    "benchmark_tests": [
      {"test_name": "llm_stream_latency_benchmark", "description": "基准测试：10 次首字节延迟，输出 P50/P95/P99", "framework": "pytest", "coverage": ["performance"]}
    ],
    "verification_commands": [
      {"layer": "python", "command": "cd Veritas/ai-service && python -m pytest tests/test_llm_stream_optimization.py -v", "expected_result": "4 个单元测试全部通过"},
      {"layer": "python", "command": "cd Veritas/ai-service && python tests/benchmark/llm_stream_latency_benchmark.py", "expected_result": "输出 Markdown 报告，P95 < 2000ms"}
    ]
  },
  "acceptance_criteria": [
    {"id": "AC-001", "criterion": "generate_stream() 日志包含 first_token_latency_ms", "verified_by": "automated_test"},
    {"id": "AC-002", "criterion": "首字节延迟 P95 < 2000ms", "verified_by": "automated_test"},
    {"id": "AC-003", "criterion": "流式失败降级为非流式，不抛出异常", "verified_by": "automated_test"},
    {"id": "AC-004", "criterion": "max_tokens/temperature 参数正确透传到 provider", "verified_by": "automated_test"},
    {"id": "AC-005", "criterion": "基准测试脚本输出 P50/P95/P99 统计报告", "verified_by": "manual_test"}
  ]
}
