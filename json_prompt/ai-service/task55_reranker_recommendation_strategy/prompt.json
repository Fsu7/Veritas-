{
  "context": {
    "project": "XH-202630 科研文献智能助手 — 领域知识个性化生成与多智能体协同决策系统研究",
    "version": "v0.5",
    "milestone": "M5 / AM5：混合检索与功能完善",
    "requirement": "完善重排序逻辑，实现 F3.4.6 推荐策略。Reranker.rerank() 支持用户画像注入，不同研究方向用户获得不同排序。新增 Reranker.recommend() 方法基于用户历史分析记录推荐相关论文。F3.4.6 推荐策略：基于用户画像 4 维度（education/research/knowledge/style）计算论文推荐分。推荐分 = rerank_score × 0.7 + recommendation_score × 0.3。RecommendationService 提供 get_recommended_papers(user_id, top_k=10) 接口。验证不同研究方向用户获得不同排序结果（AM5 验收硬指标）。本任务对应 AM5 Week 10 Day 5。",
    "requirement_ids": ["F3.2.5", "F3.4.6"],
    "reference_docs": [
      {"path": "docs/ai-service/AI服务模块系统架构文档.md", "purpose": "了解 Reranker 复合评分和 PersonalizationService 4 维度画像"},
      {"path": "docs/ai-service/AI服务模块项目里程碑文档.md", "purpose": "确认 AM5 Week 10 Day 5 推荐策略交付物和 F3.4.6 验收"},
      {"path": "AGENTS.md", "purpose": "确认个性化引擎和推荐策略规范"}
    ]
  },
  "current_architecture": {
    "involved_layers": ["python_ai_service"],
    "related_modules": [
      {"layer": "python_ai_service", "module_path": "app.services.reranker", "description": "Reranker 已实现复合评分（RRF×0.5+field×0.3+popularity×0.2+personalization_boost），但 personalization_boost 为简单加权，未实现 F3.4.6 推荐策略"},
      {"layer": "python_ai_service", "module_path": "app.services.personalization_service", "description": "PersonalizationService 已实现 4 维度画像（education_level/knowledge_level/preferred_style/research_field），F3.4.6 推荐策略未实现"},
      {"layer": "python_ai_service", "module_path": "app.services.search_service", "description": "SearchService 调用 Reranker.rerank()，需支持传入 user_profile"}
    ],
    "existing_implementations": [
      {"file_path": "Veritas/ai-service/app/services/reranker.py", "description": "Reranker.rerank() 已实现复合评分，含 personalization_boost 但未独立为推荐分", "reuse_type": "extend"},
      {"file_path": "Veritas/ai-service/app/services/personalization_service.py", "description": "PersonalizationService 已实现 get_user_profile()，需新增 get_recommendation_strategy()", "reuse_type": "extend"}
    ]
  },
  "relevant_modules": [
    {
      "name": "Reranker",
      "path": "Veritas/ai-service/app/services/reranker.py",
      "responsibility": "复合重排序",
      "key_interfaces": [
        {"name": "rerank", "signature": "def rerank(self, papers: List[Dict], query: str, user_profile: Optional[Dict] = None) -> List[Dict]", "description": "复合评分重排序，需增强 user_profile 注入"},
        {"name": "recommend", "signature": "def recommend(self, papers: List[Dict], user_profile: Dict, user_history: List[Dict]) -> List[Dict]", "description": "新增：基于用户画像和历史的推荐"}
      ]
    },
    {
      "name": "PersonalizationService",
      "path": "Veritas/ai-service/app/services/personalization_service.py",
      "responsibility": "用户画像 4 维度",
      "key_interfaces": [
        {"name": "get_user_profile", "signature": "async def get_user_profile(self, user_id: str) -> Dict", "description": "获取用户画像 4 维度"},
        {"name": "get_recommendation_strategy", "signature": "def get_recommendation_strategy(self, user_profile: Dict, paper: Dict) -> float", "description": "新增：计算论文推荐分 [0, 1]"}
      ]
    }
  ],
  "files_to_modify": [
    {"operation": "modify", "path": "Veritas/ai-service/app/services/reranker.py", "description": "1) rerank() 增强 user_profile 注入：当 user_profile 非空时，调用 personalization_service.get_recommendation_strategy() 计算推荐分，最终分 = rerank_score × 0.7 + recommendation_score × 0.3；2) 新增 recommend() 方法：基于用户画像和历史分析记录推荐相关论文，返回按推荐分排序的论文列表；3) 保持原有 personalization_boost 逻辑兼容（user_profile 为空时退化为原逻辑）。", "layer": "python_ai_service"},
    {"operation": "modify", "path": "Veritas/ai-service/app/services/personalization_service.py", "description": "新增 get_recommendation_strategy(user_profile, paper) -> float 方法：1) 基于 4 维度计算推荐分 [0, 1]；2) research_field 匹配权重 0.4（论文研究方向与用户 research_field 一致加分）；3) education_level 匹配权重 0.2（论文难度与用户教育水平匹配加分）；4) knowledge_level 匹配权重 0.2；5) preferred_style 匹配权重 0.2；6) 各维度匹配返回 [0, 1] 分数，加权求和。", "layer": "python_ai_service"},
    {"operation": "create", "path": "Veritas/ai-service/app/services/recommendation_service.py", "description": "RecommendationService：1) get_recommended_papers(user_id: str, top_k: int = 10) -> List[Dict]；2) 调用 personalization_service.get_user_profile() 获取画像；3) 获取用户历史分析记录（从 Java 后端 API 或本地缓存）；4) 候选论文池 = 历史分析论文的相似论文（通过 ChromaDB 相似检索）；5) 调用 reranker.recommend(candidates, user_profile, user_history) 排序；6) 返回 top_k 推荐论文。", "layer": "python_ai_service"},
    {"operation": "create", "path": "Veritas/ai-service/tests/test_reranker_recommendation.py", "description": "测试：1) test_rerank_with_user_profile 验证 rerank() 接受 user_profile 参数输出个性化排序；2) test_different_research_field_different_order 验证 NLP vs CV 用户 Top5 排序差异度 > 30%；3) test_recommend_method 验证 recommend() 返回推荐论文列表；4) test_recommendation_score_range 验证推荐分范围 [0, 1]；5) test_get_recommendation_strategy 验证 4 维度加权计算；6) test_recommendation_service 验证 get_recommended_papers() 接口。", "layer": "python_ai_service"}
  ],
  "implementation_requirements": {
    "functional_requirements": [
      {"id": "FR-001", "description": "Reranker.rerank() 支持用户画像注入：1) rerank(papers, query, user_profile=None) 签名保持兼容；2) 当 user_profile 非空时：a) 对每篇论文调用 personalization_service.get_recommendation_strategy(user_profile, paper) 得 recommendation_score [0,1]；b) 最终分 = rerank_score × 0.7 + recommendation_score × 0.3；c) 按最终分降序排序；3) 当 user_profile 为空时：退化为原有 personalization_boost 逻辑（兼容）。", "priority": "P0", "acceptance_condition": "Reranker.rerank() 接受 user_profile 参数，输出个性化排序"},
      {"id": "FR-002", "description": "新增 Reranker.recommend() 方法：1) 签名 recommend(papers: List[Dict], user_profile: Dict, user_history: List[Dict]) -> List[Dict]；2) 对每篇论文计算推荐分 = research_field_match × 0.4 + education_match × 0.2 + knowledge_match × 0.2 + style_match × 0.2；3) 叠加历史相似度加分（论文与用户历史分析论文的相似度）；4) 按推荐分降序排序；5) 返回排序后论文列表（含 recommendation_score 字段）。", "priority": "P1", "acceptance_condition": "Reranker.recommend() 返回推荐论文列表"},
      {"id": "FR-003", "description": "F3.4.6 推荐策略：基于用户画像 4 维度计算论文推荐分：1) research_field 匹配权重 0.4：论文 keywords/research_field 与用户 research_field 一致得 1.0，部分一致得 0.5，不一致得 0.0；2) education_level 匹配权重 0.2：论文难度（由 abstract 长度/术语密度估算）与用户教育水平匹配得 1.0，不匹配得 0.0；3) knowledge_level 匹配权重 0.2：论文技术深度与用户知识水平匹配；4) preferred_style 匹配权重 0.2：论文写作风格（理论/实验/综述）与用户偏好匹配；5) 加权求和得 recommendation_score [0, 1]。", "priority": "P0", "acceptance_condition": "F3.4.6 推荐策略基于 4 维度计算推荐分"},
      {"id": "FR-004", "description": "推荐分 = rerank_score × 0.7 + recommendation_score × 0.3：1) rerank_score 为 Reranker 复合评分（归一化到 [0, 1]）；2) recommendation_score 为 F3.4.6 推荐分 [0, 1]；3) 加权求和得最终分；4) 权重可通过 settings 配置（RERANK_WEIGHT=0.7, RECOMMENDATION_WEIGHT=0.3）。", "priority": "P1", "acceptance_condition": "推荐分计算正确，recommendation_score 范围 [0, 1]"},
      {"id": "FR-005", "description": "RecommendationService 提供 get_recommended_papers(user_id, top_k=10) 接口：1) 调用 personalization_service.get_user_profile(user_id) 获取画像；2) 获取用户历史分析记录（调用 Java 后端 /api/analysis/history 或本地缓存，本任务可 mock）；3) 候选论文池 = 历史分析论文的相似论文（ChromaDB 相似检索 top 50）；4) 调用 reranker.recommend(candidates, user_profile, user_history) 排序；5) 返回 top_k 推荐论文（含 recommendation_score 字段）。", "priority": "P1", "acceptance_condition": "RecommendationService.get_recommended_papers() 返回推荐论文列表"},
      {"id": "FR-006", "description": "验证不同研究方向用户获得不同排序结果（AM5 验收硬指标）：1) 测试用例：同一查询 + 10 篇候选论文；2) 用户A：research_field='NLP'；3) 用户B：research_field='CV'；4) 分别调用 rerank(papers, query, user_profile_A) 和 rerank(papers, query, user_profile_B)；5) 计算 Top5 排序差异度 = (Top5 中不同论文数 / 5)；6) 验证差异度 > 30%（即 Top5 中至少 2 篇不同）。", "priority": "P0", "acceptance_condition": "同一查询 NLP vs CV 用户 Top5 排序差异度 > 30%"}
    ]
  },
  "constraints": {
    "naming": {"python": {"class_name": "PascalCase", "function_variable": "snake_case", "constant": "UPPER_SNAKE_CASE", "file_name": "snake_case.py"}},
    "layering": {"python": {"rule": "重排序在 services/reranker.py，推荐在 services/recommendation_service.py，画像在 services/personalization_service.py"}},
    "error_handling": {"python": {"approach": "user_profile 为空时退化为原逻辑，不抛异常", "history_unavailable": "历史记录获取失败时候选池为空，返回空列表"}},
    "logging": {"python": "Loguru", "prohibited": ["在推荐分计算循环中打印 INFO 日志"]}
  },
  "forbidden_actions": [
    {"id": "FA-001", "action": "输出伪代码或 TODO 注释", "reason": "必须输出完整可执行代码", "severity": "critical"},
    {"id": "FA-002", "action": "删除 Reranker 原有 personalization_boost 逻辑", "reason": "user_profile 为空时需退化为原逻辑", "severity": "high"},
    {"id": "FA-003", "action": "修改 PersonalizationService 4 维度枚举值", "reason": "4 维度已定义，仅新增计算方法", "severity": "high"},
    {"id": "FA-004", "action": "在推荐分计算中调用 LLM", "reason": "推荐分基于规则计算，不调 LLM", "severity": "high"},
    {"id": "FA-005", "action": "硬编码推荐权重 0.7/0.3", "reason": "应从 settings 读取，支持环境变量覆盖", "severity": "medium"},
    {"id": "FA-006", "action": "RecommendationService 直接查 MySQL", "reason": "历史记录通过 Java 后端 API 获取，不直连 DB", "severity": "high"}
  ],
  "test_requirements": {
    "unit_tests": [
      {"test_name": "test_rerank_with_user_profile", "description": "rerank() 接受 user_profile 参数输出个性化排序", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_different_research_field_different_order", "description": "NLP vs CV 用户 Top5 排序差异度 > 30%", "framework": "pytest", "coverage": ["normal_flow", "boundary_condition"]},
      {"test_name": "test_recommend_method", "description": "recommend() 返回推荐论文列表含 recommendation_score", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_recommendation_score_range", "description": "推荐分范围 [0, 1]", "framework": "pytest", "coverage": ["normal_flow", "boundary_condition"]},
      {"test_name": "test_get_recommendation_strategy", "description": "4 维度加权计算推荐分", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_recommendation_service", "description": "get_recommended_papers() 返回推荐论文列表", "framework": "pytest", "coverage": ["normal_flow", "integration"]},
      {"test_name": "test_rerank_without_user_profile_backward_compat", "description": "user_profile 为空时退化为原逻辑", "framework": "pytest", "coverage": ["regression"]}
    ],
    "verification_commands": [
      {"layer": "python", "command": "cd Veritas/ai-service && python -m pytest tests/test_reranker_recommendation.py -v", "expected_result": "7 个测试用例全部通过"}
    ]
  },
  "acceptance_criteria": [
    {"id": "AC-001", "criterion": "Reranker.rerank() 接受 user_profile 参数，输出个性化排序", "verified_by": "automated_test"},
    {"id": "AC-002", "criterion": "同一查询，NLP 方向用户 vs CV 方向用户，Top5 排序差异度 > 30%", "verified_by": "automated_test"},
    {"id": "AC-003", "criterion": "RecommendationService.get_recommended_papers() 返回推荐论文列表", "verified_by": "automated_test"},
    {"id": "AC-004", "criterion": "推荐分计算正确，recommendation_score 范围 [0, 1]", "verified_by": "automated_test"},
    {"id": "AC-005", "criterion": "user_profile 为空时退化为原逻辑（向后兼容）", "verified_by": "automated_test"}
  ]
}
