{
  "context": {
    "project": "XH-202630 科研文献智能助手 — 领域知识个性化生成与多智能体协同决策系统研究",
    "version": "v0.5",
    "milestone": "M5 / AM5：混合检索与功能完善",
    "requirement": "将 LLM token 流通过 SSE 推送给前端，新增 token_stream 事件（第 10 种 SSE 事件，不破坏原有 9 种）。GeneratorAgent 新增 stream_generate() 方法 yield token。orchestrator 在 Generator 节点执行时调用 stream_generate() 并 yield token_stream 事件。token 流结束后 yield agent_completed 事件包含完整 report。token 流失败时降级为非流式 generate()，yield agent_failed + 降级标记。本任务对应 AM5 Week 10 Day 1-2。",
    "requirement_ids": ["F3.5.2", "F3.3.2"],
    "reference_docs": [
      {"path": "docs/ai-service/AI服务模块系统架构文档.md", "purpose": "了解 SSE 9 事件类型和 orchestrator 工作流"},
      {"path": "docs/ai-service/AI服务模块项目里程碑文档.md", "purpose": "确认 AM5 Week 10 Day 1-2 SSE token 流集成交付物"},
      {"path": "AGENTS.md", "purpose": "确认 SSE 事件规范和 Agent 降级约束"}
    ]
  },
  "current_architecture": {
    "involved_layers": ["python_ai_service"],
    "related_modules": [
      {"layer": "python_ai_service", "module_path": "app.agents.orchestrator", "description": "SSE 编排器，已实现 9 事件类型 + PING 15s + Last-Event-ID"},
      {"layer": "python_ai_service", "module_path": "app.agents.generator", "description": "GeneratorAgent 已实现 _run() 调用 LLMService.generate()，需新增 stream_generate()"},
      {"layer": "python_ai_service", "module_path": "app.api.endpoints.agent", "description": "SSE 端点 /api/agent/analyze/stream，需支持 token_stream 事件"},
      {"layer": "python_ai_service", "module_path": "app.services.llm_service", "description": "LLMService.generate_stream() 已实现（task51 已优化首字节计时）"}
    ],
    "existing_implementations": [
      {"file_path": "Veritas/ai-service/app/agents/orchestrator.py", "description": "SSEStreamOrchestrator 已实现 9 事件：agent_started/agent_completed/agent_failed/analysis_progress/analysis_completed/analysis_failed/ping/error/degraded", "reuse_type": "extend"},
      {"file_path": "Veritas/ai-service/app/agents/generator.py", "description": "GeneratorAgent._run() 调用 LLMService.generate() 非流式生成", "reuse_type": "extend"},
      {"file_path": "Veritas/ai-service/app/api/endpoints/agent.py", "description": "SSE 端点已实现，yield 9 种事件", "reuse_type": "extend"},
      {"file_path": "Veritas/ai-service/app/services/llm_service.py", "description": "LLMService.generate_stream() 已实现并优化（task51）", "reuse_type": "direct_reuse"}
    ]
  },
  "relevant_modules": [
    {
      "name": "SSEStreamOrchestrator",
      "path": "Veritas/ai-service/app/agents/orchestrator.py",
      "responsibility": "SSE 事件编排，6-Agent 工作流状态推送",
      "key_interfaces": [
        {"name": "stream_workflow", "signature": "async def stream_workflow(self, ...) -> AsyncGenerator[str, None]", "description": "主流程：yield SSE 事件，需在 Generator 节点增加 token_stream"},
        {"name": "_format_sse_event", "signature": "def _format_sse_event(self, event: str, data: dict) -> str", "description": "格式化 SSE 事件"}
      ]
    },
    {
      "name": "GeneratorAgent",
      "path": "Veritas/ai-service/app/agents/generator.py",
      "responsibility": "综述生成 Agent",
      "key_interfaces": [
        {"name": "_run", "signature": "async def _run(self, prompt: str, input_data: dict, context: dict) -> dict", "description": "非流式生成，返回完整 report"},
        {"name": "stream_generate", "signature": "async def stream_generate(self, prompt: str, input_data: dict, context: dict) -> AsyncGenerator[dict, None]", "description": "新增：流式生成，yield {token: str, is_final: bool, report: Optional[str]}"}
      ]
    }
  ],
  "files_to_modify": [
    {"operation": "modify", "path": "Veritas/ai-service/app/agents/generator.py", "description": "GeneratorAgent 新增 stream_generate() 方法：1) 调用 LLMService.generate_stream(prompt, **kwargs)；2) async for token in stream: yield {'token': token, 'is_final': False, 'report': None}；3) 流结束后拼接完整 report，yield {'token': '', 'is_final': True, 'report': full_report}；4) try-except 包裹，失败时降级调用 self._run() 并 yield 降级标记。", "layer": "python_ai_service"},
    {"operation": "modify", "path": "Veritas/ai-service/app/agents/orchestrator.py", "description": "1) 在 NODE_ORDER 的 generator 节点执行处，调用 generator.stream_generate() 而非 _run()；2) async for chunk in generator.stream_generate(): if chunk['is_final']: 缓存 report 到 state；else: yield self._format_sse_event('token_stream', {'token': chunk['token'], 'analysisId': analysis_id, 'agentName': 'generator'})；3) token 流结束后 yield agent_completed 事件包含完整 report；4) token 流失败时 yield agent_failed 事件 + degraded 标记，降级为非流式 _run()。", "layer": "python_ai_service"},
    {"operation": "modify", "path": "Veritas/ai-service/app/api/endpoints/agent.py", "description": "SSE 端点 /api/agent/analyze/stream 无需修改代码（orchestrator yield 的事件自动透传），但需在 API 文档注释中新增 token_stream 事件说明。", "layer": "python_ai_service"},
    {"operation": "create", "path": "Veritas/ai-service/tests/test_sse_token_stream.py", "description": "测试：1) test_token_stream_event_format 验证 token_stream 事件 data 含 token/analysisId/agentName；2) test_token_stream_followed_by_agent_completed 验证 token 流结束后 agent_completed 含完整 report；3) test_token_stream_fallback 验证流式失败降级为非流式；4) test_original_9_events_unchanged 验证原有 9 种事件不受影响。", "layer": "python_ai_service"}
  ],
  "implementation_requirements": {
    "functional_requirements": [
      {"id": "FR-001", "description": "新增 SSE 事件类型 token_stream：1) event='token_stream'；2) data JSON 含 token（字符串）、analysisId（分析ID）、agentName（固定为'generator'）；3) 每收到一个 LLM token 即 yield 一个 token_stream 事件；4) token 可能为空字符串（LLM 偶发），仍 yield 保持流连续性。", "priority": "P0", "acceptance_condition": "SSE 流包含 token_stream 事件，每个 token 通过 data 推送"},
      {"id": "FR-002", "description": "GeneratorAgent 新增 stream_generate() 方法：1) 签名 async def stream_generate(self, prompt: str, input_data: dict, context: dict) -> AsyncGenerator[dict, None]；2) 调用 self.llm_service.generate_stream(prompt, **kwargs)；3) async for token in stream: yield {'token': token, 'is_final': False, 'report': None}；4) 流结束后拼接所有 token 为 full_report，yield {'token': '', 'is_final': True, 'report': full_report}；5) 更新 self.status 为 completed。", "priority": "P0", "acceptance_condition": "GeneratorAgent.stream_generate() 正确 yield token 和 final report"},
      {"id": "FR-003", "description": "orchestrator 在 Generator 节点执行时调用 stream_generate()：1) async for chunk in generator.stream_generate(...): if chunk['is_final']: state['report'] = chunk['report']；else: yield self._format_sse_event('token_stream', {'token': chunk['token'], 'analysisId': analysis_id, 'agentName': 'generator'})；2) token 流结束后 yield agent_completed 事件，data 含完整 report；3) 保持原有 agent_started 事件在 stream_generate 前推送。", "priority": "P0", "acceptance_condition": "orchestrator 在 Generator 节点 yield token_stream 事件"},
      {"id": "FR-004", "description": "token 流结束后 yield agent_completed 事件包含完整 report：1) agent_completed 事件 data 含 agentName='generator'、status='completed'、result={'report': full_report}；2) full_report 为所有 token 拼接结果；3) 前端可通过 agent_completed 获取完整综述，不依赖 token_stream 拼接。", "priority": "P0", "acceptance_condition": "token 流结束后 agent_completed 事件包含完整 report"},
      {"id": "FR-005", "description": "token 流失败时降级为非流式 generate()：1) try-except 包裹 generator.stream_generate()；2) 捕获 Exception，logger.warning 输出降级原因；3) 降级调用 generator._run() 获取完整 report；4) yield agent_failed 事件含 degraded=True 标记；5) 随后 yield agent_completed 事件含降级生成的 report；6) 不中断 SSE 连接。", "priority": "P0", "acceptance_condition": "token 流失败降级为非流式，不中断 SSE 连接"},
      {"id": "FR-006", "description": "保持原有 9 种 SSE 事件不变：1) agent_started/agent_completed/agent_failed/analysis_progress/analysis_completed/analysis_failed/ping/error/degraded 保持原有格式和触发时机；2) token_stream 作为第 10 种事件新增；3) 其他 Agent（coordinator/retriever/analyzer/comparer/reviewer）仍使用 _run() 非流式，仅 generator 使用 stream_generate()。", "priority": "P1", "acceptance_condition": "原有 9 种 SSE 事件不受影响"}
    ],
    "degradation_requirements": {
      "agent_degradation": {"stream_failure": "token 流失败降级为非流式 _run()，yield agent_failed + degraded 标记", "multi_agent_failure": "N/A"}
    }
  },
  "constraints": {
    "naming": {"python": {"class_name": "PascalCase", "function_variable": "snake_case", "constant": "UPPER_SNAKE_CASE", "file_name": "snake_case.py"}},
    "layering": {"python": {"rule": "Agent 在 agents/，API 端点在 api/endpoints/，测试在 tests/"}},
    "error_handling": {"python": {"approach": "token 流失败降级为非流式，不中断 SSE", "stream_error_handling": "logger.warning + 降级到 _run()"}},
    "logging": {"python": "Loguru", "prohibited": ["在每个 token yield 时打印日志"]}
  },
  "forbidden_actions": [
    {"id": "FA-001", "action": "输出伪代码或 TODO 注释", "reason": "必须输出完整可执行代码", "severity": "critical"},
    {"id": "FA-002", "action": "修改原有 9 种 SSE 事件的格式或触发时机", "reason": "token_stream 为新增第 10 种，不破坏原有", "severity": "critical"},
    {"id": "FA-003", "action": "让其他 Agent（非 generator）也使用 stream_generate()", "reason": "仅 GeneratorAgent 流式，其他保持非流式", "severity": "high"},
    {"id": "FA-004", "action": "删除 GeneratorAgent._run() 方法", "reason": "stream_generate 失败时降级依赖 _run()", "severity": "critical"},
    {"id": "FA-005", "action": "在 token_stream 事件中推送非 token 数据（如进度）", "reason": "token_stream 专用于 LLM token，进度用 analysis_progress", "severity": "medium"},
    {"id": "FA-006", "action": "修改 LLMService.generate_stream() 签名", "reason": "task51 已优化，本任务仅消费其输出", "severity": "high"}
  ],
  "test_requirements": {
    "unit_tests": [
      {"test_name": "test_token_stream_event_format", "description": "验证 token_stream 事件 data 含 token/analysisId/agentName", "framework": "pytest", "coverage": ["normal_flow"]},
      {"test_name": "test_token_stream_followed_by_agent_completed", "description": "验证 token 流结束后 agent_completed 含完整 report", "framework": "pytest", "coverage": ["normal_flow", "integration"]},
      {"test_name": "test_token_stream_fallback", "description": "mock stream_generate 抛异常，验证降级为非流式 _run()", "framework": "pytest", "coverage": ["error_flow", "degradation"]},
      {"test_name": "test_original_9_events_unchanged", "description": "验证原有 9 种 SSE 事件格式和触发时机不变", "framework": "pytest", "coverage": ["regression"]}
    ],
    "verification_commands": [
      {"layer": "python", "command": "cd Veritas/ai-service && python -m pytest tests/test_sse_token_stream.py -v", "expected_result": "4 个测试用例全部通过"}
    ]
  },
  "acceptance_criteria": [
    {"id": "AC-001", "criterion": "SSE 流包含 token_stream 事件，每个 token 通过 data 推送", "verified_by": "automated_test"},
    {"id": "AC-002", "criterion": "token 流结束后 agent_completed 事件包含完整 report", "verified_by": "automated_test"},
    {"id": "AC-003", "criterion": "token 流失败降级为非流式，不中断 SSE 连接", "verified_by": "automated_test"},
    {"id": "AC-004", "criterion": "原有 9 种 SSE 事件格式和触发时机不变", "verified_by": "automated_test"}
  ]
}
