spring:
  datasource:
    url: jdbc:mysql://localhost:3306/literature_assistant_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: Aa2105268075.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-jwt-secret-key-for-unit-testing-at-least-32-chars
  expiration: 86400000

ai-service:
  url: http://localhost:8000

cors:
  allowed-origins: http://localhost:5173

logging:
  level:
    root: WARN
    com.literatureassistant: DEBUG
